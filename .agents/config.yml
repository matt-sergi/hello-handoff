# .agents/config.yml — Generated by /handoff setup
# Re-run /handoff setup to modify any of these values.

project:
  name: "hello-handoff"
  description: ""
  languages: []
  frameworks: []

autonomy:
  default: "full"        # full | plan | supervised
  labels:
    full: "autonomy:full"
    plan: "autonomy:plan"
    supervised: "autonomy:supervised"

identity:
  # Each human is a key under identity. Agents are namespaced by human
  # to prevent collisions when multiple people share a repo.
  # Labels: <human>:<agent-name>  Branches: <human>/<agent-name>
  # Worktrees: ../<project>-<human>-<agent-name>
  #
  # Manage agents dynamically: /handoff agent add <name>
  matt-sergi:
    agents:
      - name: "agent-1"
        worktree: "../hello-handoff-matt-sergi-agent-1"

git:
  provider: "github"                    # Only GitHub supported currently
  org: "matt-sergi"
  repo: "hello-handoff"
  default-branch: "main"
  branch-pattern: "task/ISSUE-{number}-{description}"
  commit-format: "conventional"         # conventional | freeform

tracker:
  provider: "github"                    # github | jira | linear
  github:
    project-number: 1
    owner: "matt-sergi"
    field-id: "PVTSSF_lAHOD51gHs4BPYw9zg9z2JI"
    statuses:
      backlog: "d961d47f"
      ready: "4ea244e1"
      in-progress: "3de44a86"
      in-review: "6ce3c407"
      in-human-review: "f856bf31"
      needs-revision: "1110bda5"
      done: "bf13acef"
