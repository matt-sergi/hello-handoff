name: linting
required: true
description: "Static analysis to catch bugs and enforce code style"

gates:
  - name: "eslint"
    command: "npx eslint ."
    pass-condition: "exit-code-zero"
    description: "Run ESLint on all source files"
    run: "before-pr"
